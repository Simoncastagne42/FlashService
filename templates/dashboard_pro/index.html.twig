{% extends 'base.html.twig' %}

{% block title %}Dashboard Pro{% endblock %}

{% block content %}
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold">Bienvenue sur ton Dashboard Pro</h1>
        <p class="text-gray-600">Gère tes services, créneaux et réservations ici.</p>

        <div class="grid grid-cols-3 gap-4 mt-4">
            <a href="{{ path('service_index') }}" class="p-4 bg-blue-500 text-white rounded">Mes Services</a>
            <a href="{{ path('time_slot_index') }}" class="p-4 bg-green-500 text-white rounded">Mes Créneaux</a>
            <a href="{{ path('reservation_index') }}" class="p-4 bg-red-500 text-white rounded">Mes Réservations</a>
        </div>
    </div>
{% endblock %}